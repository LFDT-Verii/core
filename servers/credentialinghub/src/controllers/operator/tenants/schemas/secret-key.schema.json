{
  "title": "Secret key Value",
  "$id": "secret-key",
  "description": "A secret key",
  "oneOf": [
    {
      "type": "object",
      "properties": {
        "key": {
          "type": "string"
        },
        "encoding": {
          "type": "string",
          "enum": ["hex"]
        }
      },
      "required": ["key"]
    },
    {
      "type": "object",
      "properties": {
        "jwk": {
          "$ref": "jwk#"
        },
        "encoding": {
          "type": "string",
          "enum": ["jwk"]
        }
      },
      "required": ["jwk"]
    }
  ]
}
