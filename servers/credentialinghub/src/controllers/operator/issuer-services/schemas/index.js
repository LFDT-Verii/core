const newIssuerConfigurationSchema = require('./new-issuer-service.schema');
const issuerConfigurationSchema = require('./issuer-service.schema');

module.exports = {
  issuerConfigurationSchema,
  newIssuerConfigurationSchema,
};
