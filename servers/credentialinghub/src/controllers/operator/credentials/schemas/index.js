const newCredentialSchema = require('./new-credential.schema.json');
const credentialSchema = require('./credential.schema');
const revokeCredentialSchema = require('./revoke-credential.schema.json');

module.exports = {
  credentialSchema,
  newCredentialSchema,
  revokeCredentialSchema,
};
